"""
字典
存储无序数据集合，因为列表自带索引值，所以可以看作是有序数据
字典定义{}
使用键值对存储数据，键值对之间使用，分隔（key:value)
键必须是唯一的，只能使用字符串、数字或元组
1.取值
字典名【key】
2.修改
字典名【key】=需要增加的值
字典名【key】=需要改成的值
3.删除
字典名.pop（key)
4.统计键值对数量
len(字典名）
5.合并字典
字典名.update(另外一个字典名)
6.清空字典
字典名.clear
7.字典循环遍历
变量k是每一次循环中，获取到到的键值对的key
for k in 字典名

"""
xi={"name":"小明",
    "age":"19",
    "gen":"true",
    "hi":"175"
    }
xi["name"]="小红"
print(xi)
xi.pop("name")
print(xi)
print(len(xi))
for k in xi:
    print(k,xi[k])

#多个字典放在列表中
list=[
    {"name":2,
     "d":33},
    {"lk":111,
     "ooo":999
     }
]
for o in list:
    print(o)
