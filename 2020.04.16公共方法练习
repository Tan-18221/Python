"""
公共方法
1。内置函数
len()计算容器中元素个数
del()删除变量
max()返回容器中最大值
min()返回容器最小值
cmp()比较两个值

2。切片
【变量】【开始索引值：结束索引值】

3。运算符
+ 合并
* 重复（只能列表元组字符串）
in 元素（序列）返回true 判断是否在数据里面
not in

4。for循环用法
for 变量 in 集合：
    循环体
else：(循环不执行完毕，else就不会执行）
    没有通过break退出循环
"""
for num in [1,2,3]:
    print(num)
    if num ==2:
        break
else:
    #如果循环体内使用break退出循环 else下方代码就不会执行
    print("zhixing ")
print("循环结束")

"""
for else循环
"""
