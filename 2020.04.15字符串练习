" "
当字符串中有" "时候，要用' '定义
字符串里面也是有索引，一个个取出来
字符串名【索引值】
循环遍历
for char in 字符串名
1。统计长度
len(字符串名)
2。计算指定元素出现次数
字符串名.count（元素）
3。某一个子字符串出现的位置
字符串名.index（"子字符串"）
4。判断空白字符
字符串.isspace()返回true
5。判断包含数字方法,不能判断小数
字符串.isdecimal()
字符串.isdigit() 能力强大 都能判断
字符串.isnumeric()还可以判断中文数字
6。字符串查找替换

"""
str="hello"
print(len(str))
print(str.count("l"))
print(str.index("lo"))
